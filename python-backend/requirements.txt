# Flask web framework
Flask==3.0.0
flask-cors==4.0.0
Werkzeug==3.0.1

# FFmpeg Python wrapper for video/audio processing
ffmpeg-python==0.2.0

# HTTP requests for API integrations
requests==2.31.0

# Basic utilities
gunicorn==21.2.0
python-dotenv==1.0.0

# Note: FFmpeg binary installed via Dockerfile apt-get
# Heavy AI models removed to ensure Railway deployment success
# Can be added incrementally or via API integrations
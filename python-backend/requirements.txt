# Flask web framework
Flask==3.0.0
flask-cors==4.0.0
Werkzeug==3.0.1

# FFmpeg Python wrapper for video/audio processing
ffmpeg-python==0.2.0

# Basic utilities
gunicorn==21.2.0
python-dotenv==1.0.0

# Note: FFmpeg binary will be installed by Railway's Nixpacks automatically
# Heavy AI models (Spleeter, DeepFilterNet) removed to ensure successful Railway deployment
# These can be added back incrementally or replaced with API integrations
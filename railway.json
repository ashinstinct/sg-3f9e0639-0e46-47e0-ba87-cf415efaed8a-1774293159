{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd python-backend && pip install --no-cache-dir -r requirements.txt && chmod +x start.sh"
  },
  "deploy": {
    "startCommand": "cd python-backend && ./start.sh",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}